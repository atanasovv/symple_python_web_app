apiVersion: v1
kind: Service
metadata:
    name: "vakicluster-886e"
    labels:
        app: "vakicluster-886e"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "vakicluster-886e"
